name: PR Tests

on:
  pull_request:
  workflow_dispatch:

env:
  COMPOSE_PROJECT_NAME: tandotests

jobs:
  fast_tests:
    name: Fast Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Run fast tests
        run: dotnet test Plugins/BTCPayServer.Plugins.Tando.Tests --filter "FastTest=FastTest" --logger "trx;LogFileName=fast-tests.trx" --results-directory /tmp/TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: fast-tests-results
          path: /tmp/TestResults
          if-no-files-found: ignore

  integration_tests:
    name: Integration Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      # Postgres, NBXplorer, regtest bitcoind, and the merchant/customer Lightning
      # nodes - same stack docs/Setup.md has contributors bring up locally.
      - name: Start regtest dependencies
        run: |
          cd btcpayserver/BTCPayServer.Tests
          chmod +x *.sh
          docker compose up -d dev

      - name: Run integration tests
        run: dotnet test Plugins/BTCPayServer.Plugins.Tando.Tests --filter "Integration=Integration" --logger "trx;LogFileName=integration-tests.trx" --results-directory /tmp/TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: integration-tests-results
          path: /tmp/TestResults
          if-no-files-found: ignore

      - name: Tear down dependencies
        if: always()
        run: |
          cd btcpayserver/BTCPayServer.Tests
          docker compose down -v

  playwright_tests:
    name: Playwright Tests
    runs-on: ubuntu-24.04
    timeout-minutes: 25
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Start regtest dependencies
        run: |
          cd btcpayserver/BTCPayServer.Tests
          chmod +x *.sh
          docker compose up -d dev

      - name: Build test project (needed before installing Playwright browsers)
        run: dotnet build Plugins/BTCPayServer.Plugins.Tando.Tests

      - name: Install Playwright browsers
        run: |
          PLAYWRIGHT_PS1=$(find Plugins/BTCPayServer.Plugins.Tando.Tests/bin -name "playwright.ps1" | head -n 1)
          pwsh "$PLAYWRIGHT_PS1" install --with-deps chromium

      - name: Run Playwright tests (both batches)
        run: dotnet test Plugins/BTCPayServer.Plugins.Tando.Tests --filter "Playwright=Playwright|Playwright=Playwright-2" --logger "trx;LogFileName=playwright-tests.trx" --results-directory /tmp/TestResults

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-tests-results
          path: /tmp/TestResults
          if-no-files-found: ignore

      - name: Tear down dependencies
        if: always()
        run: |
          cd btcpayserver/BTCPayServer.Tests
          docker compose down -v
